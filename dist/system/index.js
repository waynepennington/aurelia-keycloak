'use strict';

System.register(['./aurelia-keycloak'], function (_export, _context) {
  "use strict";

  return {
    setters: [function (_aureliaKeycloak) {
      var _exportObj = {};

      for (var _key in _aureliaKeycloak) {
        if (_key !== "default") _exportObj[_key] = _aureliaKeycloak[_key];
      }

      _export(_exportObj);
    }],
    execute: function () {}
  };
});